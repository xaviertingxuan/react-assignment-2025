// import create from 'zustand'

// const useJwt = create((set) => ({
//   jwt: null,
//   setJwt: (token) => set({ jwt: token }),
//   getJwt: () => useJwt.getState().jwt,
//   clearJwt: () => set({ jwt: null }) // Add this line for logout
// }))

// export { useJwt }